monitor=,preferred,auto,auto
monitor=eDP-1, preferred,auto,1.2 # laptop monitor
monitor=DP-5, preferred,auto-left,auto # home LG monitor

$terminal = ghostty
$fileManager = nautilus
$menu = tofi-drun --drun-launch=true --width=25% --height=50%

exec-once = $terminal
exec-once = zen-twilight
exec-once = obsidian & steam & spotify & webcord & mullvad-gui

general {
    gaps_in = 6
    gaps_out = 10
    border_size = 1
    resize_on_border = true

    col.active_border = rgba(9aa5ce70)
    col.inactive_border = rgba(595959aa)
    allow_tearing = true
    layout = dwindle
}

decoration {
    rounding = 5
    rounding_power = 4
    inactive_opacity = 0.95

    blur {
        enabled = true
        size = 8
    }

    shadow {
        enabled = false
    }
}

xwayland {
    force_zero_scaling = true
}

animations {
    enabled = yes, please :)

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, quick
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, quick, fade
    animation = workspacesIn, 1, 1.21, quick, fade
    animation = workspacesOut, 1, 1.94, quick, fade
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

input {
    kb_layout = us
    kb_options =
    kb_rules =

    follow_mouse = 1
    # accel_profile = flat
    # sensitivity = 0.50
    sensitivity = 0.25

    touchpad {
        natural_scroll = true
	scroll_factor = 0.5
	middle_button_emulation = true
	drag_lock = 0
    }

}

device {
    name = cx-gaming-mouse
    sensitivity = 0.2
    accel_profile = flat
    middle_button_emulation = false
}

$mainMod = SUPER

bind = $mainMod, C, killactive,
bind = $mainMod, V, togglefloating,
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod, O, exec, hyprlock
bind = $mainMod SHIFT, O, exec, systemctl hibernate
bind = , PRINT, exec, hyprshot -m region --clipboard-only

bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Numpad bindings
bind = , KP_End, workspace, 1
bind = , KP_Down, workspace, 2
bind = , KP_Next, workspace, 3
bind = , KP_Left, workspace, 4
bind = , KP_Begin, workspace, 5
bind = , KP_Right, workspace, 6
bind = , KP_Home, workspace, 7
bind = , KP_Up, workspace, 8
bind = , KP_Prior, workspace, 9
bind = , KP_Insert, workspace, 10

bind = $mainMod SHIFT, M, exec, wpctl set-default $(wpctl status | grep "Sinks" -A 6 -m 1 | sed -n '/Sinks/,/Sources/p' | sed '1d;$d;$d' | sed '$d' | tofi | grep -Eo "([1-9][0-9]*)\." | sed 's/\.//')
bind = $mainMod SHIFT, B, exec, blueman-manager

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, Control_L, movewindow

bindm = $mainMod, mouse:273, resizewindow
bindm = $mainMod, ALT_L, resizewindow

bindel = ,XF86AudioRaiseVolume, exec, /home/mukize/.config/hypr/scripts/dunst_osd.sh volume_up
bindel = ,XF86AudioLowerVolume, exec, /home/mukize/.config/hypr/scripts/dunst_osd.sh volume_down
bindel = ,XF86AudioMute, exec, /home/mukize/.config/hypr/scripts/dunst_osd.sh volume_mute
# bindel = ,XF86AudioMicMute, exec, /home/mukize/.config/hypr/scripts/dunst_osd.sh volume_mute
bindel = ,XF86MonBrightnessUp, exec, /home/mukize/.config/hypr/scripts/dunst_osd.sh brightness_up
bindel = ,XF86MonBrightnessDown, exec, /home/mukize/.config/hypr/scripts/dunst_osd.sh brightness_down

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

bind = $mainMod, Q, workspace, 4 # Office workspace
bind = $mainMod, W, workspace, 5 # Office workspace
bind = $mainMod, E, workspace, 6
bind = $mainMod, A, workspace, 7
bind = $mainMod, S, workspace, 8 # Spotify workspace
bind = $mainMod, D, workspace, 9 # Discord workspace
bind = $mainMod, R, workspace, 10 # Steam workspace

windowrule = workspace 2, class:zen-twilight
windowrule = workspace 3, class:obsidian
windowrule = workspace 4, class:wpsoffice
windowrule = workspace 4, class:ONLYOFFICE
windowrule = workspace 7, class:REAPER
windowrule = workspace 8, class:spotify
windowrule = workspace 9, class:WebCord
windowrule = workspace 10, class:steam

windowrule = float, title:(Picture-in-Picture)
windowrule = pin, title:(Picture-in-Picture)
windowrule = noinitialfocus, title:(Picture-in-Picture)
windowrule = persistentsize, title:(Picture-in-Picture)
windowrule = size 25% 25%, title:(Picture-in-Picture)
windowrule = move 100%-w-20 100%-w-20, title:(Picture-in-Picture)
windowrule = size 60% 60%, class:zen-twilight, floating:1

windowrule = float, title:(Create Virtual Machine)

# windowrule = immediate, class:^steam\S*
windowrulev2 = fullscreen,class:^steam_app_\d+$
workspace = 10, border:false, rounding:false
bind = $mainMod SHIFT, F, fullscreen

windowrule = float, class:org.pulseaudio.pavucontrol
windowrule = size 60% 60%, class:org.pulseaudio.pavucontrol
windowrule = float, class:.blueman-manager-wrapped
windowrule = size 50% 75%, class:.blueman-manager-wrapped
windowrule = float, class:xdg-desktop-portal-gtk
windowrule = float, class:QjackCtl
windowrule = float, class:msiexec.exe

windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0 # Fix some dragging issues with XWayland
windowrule = suppressevent maximize, class:.*

env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
